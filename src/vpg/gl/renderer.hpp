#pragma once

namespace vpg::gl {
    // What does the renderer do?
    // It takes the scene graph as input and:
    // - draws all meshes
    // - applies effects
    // - draws particle systems (? maybe)
    // - draws the GUI

    class Renderer {
    public:

    private:

    };
}